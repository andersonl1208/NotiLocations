<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <SwitchPreferenceCompat
        android:key="dark_theme"
        android:title="Enable Dark Theme" />

    <SwitchPreferenceCompat
        android:key="voice_notification"
        android:title="Read notifications aloud" />

    <SeekBarPreference
        android:key="distance"
        android:max="10"
        android:title="Default distance to trigger notification"
        app:min="1"
        app:showSeekBarValue="true" />

    <SeekBarPreference
        android:key="max_speed"
        android:max="100"
        android:title="Default max speed for notification (mph)"
        app:min="25"
        app:showSeekBarValue="true" />

</PreferenceScreen>